INCLUDE_DIRECTORIES(${DEPS_INCLUDE_DIRS})
LINK_DIRECTORIES(${DEPS_LIBRARY_DIRS})

set (th_src
    main.c
)

SET(CMAKE_C_FLAGS "-std=c99 -Wall")

ADD_EXECUTABLE (termhome ${th_src})
INSTALL(TARGETS termhome RUNTIME DESTINATION bin)
TARGET_LINK_LIBRARIES (termhome ${DEPS_LIBRARIES})

